En este proyecto se utiliza las dependecias que aparecen en package.json las cuales se omiten a ser subidas
al repositorio de git ya que se pueden instalar todas usando el comando "nmp i" y se instalaran todas las dependecias
necesarias, tambien se excluye la carpeta .vscode con informacion de personalizacion del puerto de mi liveserver.
